/** Versi rilis VANDOR — sinkronkan dengan package.json saat bump. */
export const APP_VERSION = "3.2.0";

export const APP_NAME = "VANDOR";

export const APP_USER_AGENT = `${APP_NAME}/${APP_VERSION} (personal-assistant)`;
